# 🎯 Автоматический деплой - Одна команда!

## Самый быстрый способ (рекомендуется)

Запустите автоматический скрипт:

```bash
./SETUP_NOW.sh
```

Скрипт автоматически:
1. ✅ Установит зависимости
2. ✅ Создаст SSH ключ
3. ✅ Настроит GitHub Secrets
4. ✅ Создаст и запушит PR

**Единственное, что нужно вручную:**
- Ввести GitHub токен (Personal Access Token с правами `repo`)
- Вставить SSH ключ (скрипт его покажет)
- Смержить PR после проверки

---

## Ручная настройка (если нужно больше контроля)

### Шаг 1: Создать SSH ключ

```bash
ssh-keygen -t ed25519 -C "github-actions-deploy" -f ~/.ssh/github_deploy
cat ~/.ssh/github_deploy.pub >> ~/.ssh/authorized_keys
cat ~/.ssh/github_deploy  # Скопировать вывод!
```

### Шаг 2: Установить секреты

```bash
pip3 install requests PyNaCl
python3 scripts/setup_secrets.py
```

Ввести:
- Token: ваш GitHub Personal Access Token
- HOST: (автоматически определится)
- USER: `ubuntu` (Enter)
- PORT: `22` (Enter)
- KEY: вставить из шага 1, Ctrl+D

### Шаг 3: Создать PR

```bash
git restore .
git checkout -b feature/setup-ci-cd
git add .github/ *.md scripts/ SETUP_NOW.sh
git commit -m "feat: настроить CI/CD"
git push -u origin feature/setup-ci-cd
gh pr create --title "CI/CD setup" --body "Auto-deploy setup"
```

---

## После настройки

**Автоматический workflow:**
1. Создаете feature-ветку → пишете код
2. Пушите → создаете PR
3. Мержите в main
4. 🚀 GitHub Actions автоматически деплоит на сервер!

**Проверить деплой:**
- Логи GitHub Actions: https://github.com/maddness/vkusvill-mcp-bot/actions
- Логи на сервере: `podman-compose logs -f`
- Секреты: https://github.com/maddness/vkusvill-mcp-bot/settings/secrets/actions

---

## Что происходит при деплое

```bash
# GitHub Actions выполняет на сервере:
cd /home/ubuntu/vkusvill-mcp-bot
git pull origin main
podman-compose down
podman-compose up -d --build
```

---

## Запуск деплоя вручную

Можно запустить деплой без push через GitHub UI:
https://github.com/maddness/vkusvill-mcp-bot/actions/workflows/deploy.yml
→ Run workflow → Run workflow

---

## Дополнительные документы

- `DEPLOYMENT.md` - Полная документация по CI/CD
- `DEPLOY_SETUP.md` - Подробная инструкция настройки
- `QUICKSTART.md` - Краткая шпаргалка
- `scripts/setup_secrets.py` - Скрипт для настройки секретов

---

## Требования

- Python 3.6+
- pip (для установки зависимостей)
- git
- SSH доступ к серверу
- GitHub токен с правами `repo`

---

## Безопасность

✅ SSH ключ создается специально для деплоя  
✅ Секреты шифруются перед отправкой  
✅ Токен GitHub с минимальными правами  
✅ Деплой только из main ветки  
✅ SSH подключение по ключу, не по паролю

---

## Troubleshooting

**Q: Скрипт не находит python3**  
A: Установите Python: `sudo apt install python3 python3-pip`

**Q: Ошибка 401 при настройке секретов**  
A: Проверьте GitHub токен, должен иметь scope `repo`

**Q: SSH ошибка при деплое**  
A: Проверьте, что публичный ключ в `~/.ssh/authorized_keys`

**Q: Деплой не запускается**  
A: Проверьте секреты на GitHub и логи Actions

**Q: Как отменить настройку?**  
A: Удалите секреты на GitHub и ветку `feature/setup-ci-cd`

---

## 🎉 Готово к продакшену!

После настройки каждый merge в main будет автоматически деплоить код на сервер.
Никаких ручных действий, никакого SSH - просто мержите PR! 🚀

