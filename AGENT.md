# VkusVill AI Shopping Assistant

Telegram-бот для автоматического сбора продуктовых корзин ВкусВилл с использованием AI.

![Demo](assets/pic.jpg)

## 🎯 Возможности

- 🤖 **AI-ассистент** на базе Claude Haiku 4.5 для понимания рецептов
- 🔍 **Автоматический поиск** товаров через MCP (Model Context Protocol)
- 🛒 **Генерация корзин** с готовой ссылкой на оформление заказа
- ⚡ **Стриминг ответов** в реальном времени (Bot API 9.3)
- 📝 **Контекстная память** последних 20 сообщений
- 👥 **Уведомления админов** о всех запросах пользователей

## 🏗️ Архитектура

### Технологический стек

- **[OpenAI Agents SDK](https://github.com/openai/openai-agents-python)** — агентский фреймворк с поддержкой MCP
- **[LiteLLM](https://github.com/BerriAI/litellm)** — прокси для работы с разными LLM
- **[aiogram](https://github.com/aiogram/aiogram)** — асинхронный Telegram Bot API
- **Claude Haiku 4.5** — языковая модель
- **Docker** — контейнеризация

### Структура проекта

```
vkusvill-mcp-bot/
├── app.py                      # Точка входа
├── config.yaml                 # Конфигурация
├── prompts/                    # AI промпты
│   ├── system_prompt.txt       # Системный промпт
│   └── user_initial_prompt.txt # Начальный промпт пользователя
├── bot/src/
│   ├── handlers/               # Telegram обработчики
│   │   ├── commands.py         # Команды (/start, /new_chat)
│   │   └── messages.py         # Обработка сообщений + стриминг
│   ├── agent/                  # AI Agent
│   │   └── runner.py           # Запуск агента, стриминг LLM
│   ├── mcp/                    # MCP клиент
│   │   ├── client.py           # HTTP клиент для MCP
│   │   └── tools.py            # Инструменты (search, create_cart)
│   └── utils/
│       └── config.py           # Загрузчик конфигурации
└── assets/                     # Ресурсы

```

## 🚀 Быстрый старт

### Требования

- Python 3.11+
- Docker (опционально)
- Telegram Bot Token от [@BotFather](https://t.me/BotFather)
- API ключ для Claude (или совместимого API)

### Установка

1. **Клонировать репозиторий**

```bash
git clone https://github.com/maddness/vkusvill-mcp-bot.git
cd vkusvill-mcp-bot
```

2. **Настроить конфигурацию**

```bash
cp config.example.yaml config.yaml
```

Заполнить `config.yaml`:

```yaml
telegram:
  bot_token: "YOUR_BOT_TOKEN"
  admin_ids:
    - 123456789  # Ваш Telegram ID

llm:
  model: "litellm/openai/claude-haiku-4-5"
  api_key: "YOUR_API_KEY"
  api_base: "https://openai-hub.neuraldeep.tech/v1"

mcp:
  url: "https://mcp001.vkusvill.ru/mcp"

bot:
  max_history_messages: 20
  stream_update_interval: 1.0
  stream_min_chars: 50
```

3. **Запуск**

**Через Docker (рекомендуется):**

```bash
docker compose up -d
docker compose logs -f
```

**Локально:**

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python app.py
```

## 💬 Использование

### Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Начать работу с ботом |
| `/new_chat` | Сбросить контекст разговора |
| `/new_topic [название]` | Создать новую тему (требует Bot API 9.3) |

### Примеры запросов

```
👤 Собери корзину для оливье
🤖 [Бот находит все ингредиенты и создает корзину]

👤 Хочу приготовить пасту карбонара
🤖 [Бот подбирает продукты для карбонары]

👤 Добавь еще сыр пармезан
🤖 [Бот ищет дополнительные ингредиенты]
```

## 🔧 Конфигурация

### config.yaml

```yaml
telegram:
  bot_token: "..."              # Токен Telegram бота
  admin_ids: [123, 456]         # ID администраторов

llm:
  model: "..."                  # Модель LLM
  api_key: "..."                # API ключ
  api_base: "..."               # Базовый URL API

mcp:
  url: "..."                    # URL MCP сервера

bot:
  max_history_messages: 20      # Макс. сообщений в истории
  stream_update_interval: 1.0   # Интервал обновления стрима (сек)
  stream_min_chars: 50          # Мин. символов для обновления
```

### Промпты

Промпты хранятся в `prompts/`:

- **system_prompt.txt** — системный промпт для AI
- **user_initial_prompt.txt** — шаблон начального сообщения

Можно редактировать без изменения кода!

## 🎨 Особенности

### Стриминг ответов

Бот поддерживает стриминг ответов в реальном времени:

1. Использует Bot API 9.3 `sendMessageDraft` (если доступно)
2. Fallback на `editMessageText` с защитой от flood control
3. Обновление каждые 50 символов или раз в секунду

### MCP Tools

Бот использует два инструмента через MCP:

1. **search_products(query)** — поиск товаров по названию
2. **create_cart(products_json)** — создание корзины и получение ссылки

### Контекстная память

- Хранит последние 20 сообщений (10 пар запрос-ответ)
- Отдельный контекст для каждого пользователя
- Поддержка топиков (отдельный контекст для каждой темы)

## 🔐 Безопасность

- Конфигурация в `.gitignore`
- Токены и API ключи не в коде
- SSL verification отключен только для MCP (внутренний сервис)
- Уведомления админов о всех запросах

## 📊 Мониторинг

### Логирование

```bash
# Docker
docker compose logs -f

# Локально
tail -f bot.log
```

### Метрики

Бот логирует:
- 👤 Запросы пользователей
- 🔍 Поиски товаров
- 🛒 Создание корзин
- 📊 Использование токенов
- ⚠️ Ошибки

## 🚢 Деплой

### Docker Compose

```yaml
services:
  vkusvill-bot:
    build: .
    container_name: vkusvill-bot
    restart: unless-stopped
    volumes:
      - ./app.py:/app/app.py
      - ./bot:/app/bot
      - ./config.yaml:/app/config.yaml
      - ./prompts:/app/prompts
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
```

### Управление

```bash
# Запуск
docker compose up -d

# Остановка
docker compose down

# Перезапуск
docker compose restart

# Логи
docker compose logs -f

# Обновление
git pull
docker compose up -d --build
```

## 🛠️ Разработка

### Добавление нового handler

1. Создать функцию в `bot/src/handlers/`
2. Добавить router
3. Зарегистрировать в `app.py`

### Добавление нового MCP tool

1. Добавить функцию в `bot/src/mcp/tools.py`
2. Декорировать `@function_tool`
3. Добавить в список tools

### Изменение промптов

Просто отредактируйте файлы в `prompts/`:
- `system_prompt.txt` — поведение AI
- `user_initial_prompt.txt` — начальное сообщение

Перезапустите бота для применения изменений.

## 🐛 Troubleshooting

### Бот не отвечает

1. Проверьте логи: `docker compose logs`
2. Проверьте токен бота в `config.yaml`
3. Убедитесь, что бот запущен: `docker compose ps`

### Ошибка "Flood control exceeded"

Это нормально при частых обновлениях. Бот автоматически снижает частоту.

### Топики не работают

1. Включите топики через @BotFather:
   - `/mybots` → выбрать бота
   - Bot Settings → Topics in Private Chats
2. Обновите Telegram до последней версии

### MCP ошибки

Проверьте доступность MCP сервера:
```bash
curl https://mcp001.vkusvill.ru/mcp
```

## 📝 Лицензия

MIT

## 🤝 Контрибьюция

Pull requests приветствуются! Для крупных изменений создайте issue для обсуждения.

## 📧 Контакты

- GitHub: [maddness/vkusvill-mcp-bot](https://github.com/maddness/vkusvill-mcp-bot)
- Issues: [GitHub Issues](https://github.com/maddness/vkusvill-mcp-bot/issues)

## 🙏 Благодарности

- [OpenAI Agents SDK](https://github.com/openai/openai-agents-python)
- [LiteLLM](https://github.com/BerriAI/litellm)
- [aiogram](https://github.com/aiogram/aiogram)
- [ВкусВилл](https://vkusvill.ru/) за MCP API

---

**Версия:** 2.0.0  
**Дата обновления:** 31 декабря 2025  
**Bot API:** 9.3

