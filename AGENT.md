# VkusVill AI Shopping Assistant

Telegram-бот для автоматического сбора продуктовых корзин ВкусВилл с использованием AI.

![Demo](assets/pic.jpg)

## 🎯 Возможности

- 🤖 **AI-ассистент** на базе Claude Haiku 4.5 для понимания рецептов
- 📸 **Vision (multimodal)** — анализ фото холодильника, продуктов и блюд
- 🔍 **Автоматический поиск** товаров через MCP (Model Context Protocol)
- 🛒 **Генерация корзин** с готовой ссылкой на оформление заказа
- ⚡ **Стриминг ответов** в реальном времени (Bot API 9.3)
- 📝 **Контекстная память** последних 20 сообщений
- 👥 **Уведомления админов** о всех запросах пользователей
- 📊 **Логирование взаимодействий** в структурированном JSON формате
- 💾 **База данных пользователей** с статистикой использования
- 🎙️ **Транскрибация голосовых** сообщений через Whisper API

## 🏗️ Архитектура

### Технологический стек

- **[OpenAI Agents SDK](https://github.com/openai/openai-agents-python)** — агентский фреймворк с поддержкой MCP
- **[LiteLLM](https://github.com/BerriAI/litellm)** — прокси для работы с разными LLM
- **[aiogram](https://github.com/aiogram/aiogram)** — асинхронный Telegram Bot API
- **Claude Haiku 4.5** — языковая модель
- **Docker** — контейнеризация

### Структура проекта

```
vkusvill-mcp-bot/
├── app.py                      # Точка входа
├── config.yaml                 # Конфигурация
├── prompts/                    # AI промпты
│   ├── system_prompt.txt       # Системный промпт
│   └── user_initial_prompt.txt # Начальный промпт пользователя
├── bot/src/
│   ├── handlers/               # Telegram обработчики
│   │   ├── commands.py         # Команды (/start, /new_chat)
│   │   └── messages.py         # Обработка сообщений + стриминг
│   ├── agent/                  # AI Agent
│   │   └── runner.py           # Запуск агента, стриминг LLM
│   ├── mcp/                    # MCP клиент
│   │   ├── client.py           # HTTP клиент для MCP
│   │   └── tools.py            # Инструменты (search, create_cart)
│   └── utils/
│       ├── config.py           # Загрузчик конфигурации
│       ├── logger.py           # Логирование взаимодействий
│       └── database.py         # База данных пользователей
├── logs/                       # Логи взаимодействий (YYYY-MM-DD/user_id/)
├── db/                         # База данных (users.json, interactions.json)
└── assets/                     # Ресурсы

```

## 🚀 Быстрый старт

### Требования

- Python 3.11+
- Docker (опционально)
- Telegram Bot Token от [@BotFather](https://t.me/BotFather)
- API ключ для Claude (или совместимого API)

### Установка

1. **Клонировать репозиторий**

```bash
git clone https://github.com/maddness/vkusvill-mcp-bot.git
cd vkusvill-mcp-bot
```

2. **Настроить конфигурацию**

```bash
cp config.example.yaml config.yaml
```

Заполнить `config.yaml`:

```yaml
telegram:
  bot_token: "YOUR_BOT_TOKEN"
  admin_ids:
    - 123456789  # Ваш Telegram ID

llm:
  model: "litellm/openai/claude-haiku-4-5"
  api_key: "YOUR_API_KEY"
  api_base: "YOUR_API_BASE_URL"  # URL вашего LLM API

mcp:
  url: "https://mcp001.vkusvill.ru/mcp"

bot:
  max_history_messages: 20
  stream_update_interval: 1.0
  stream_min_chars: 50
```

3. **Запуск**

**Через Docker (рекомендуется):**

```bash
docker compose up -d
docker compose logs -f
```

**Локально:**

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python app.py
```

## 💬 Использование

### Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Начать работу с ботом |
| `/new_chat` | Сбросить контекст разговора |
| `/new_topic [название]` | Создать новую тему (требует Bot API 9.3) |

### Примеры запросов

**Текстовые запросы:**
```
👤 Собери корзину для оливье
🤖 [Бот находит все ингредиенты и создает корзину]

👤 Хочу приготовить пасту карбонара
🤖 [Бот подбирает продукты для карбонары]

👤 Добавь еще сыр пармезан
🤖 [Бот ищет дополнительные ингредиенты]
```

**С фотографиями:**
```
👤 [отправляет фото холодильника] Что приготовить?
🤖 Вижу у тебя есть яйца, молоко, овощи. Предлагаю приготовить омлет!
    [Создаёт корзину с недостающими ингредиентами]

👤 [фото продуктов] Чего не хватает для борща?
🤖 На фото вижу свеклу, капусту. Для борща нужно докупить...
    [Ищет недостающие ингредиенты]
```

**Голосовые сообщения:**
```
👤 🎙️ [голосовое: "Собери мне корзину для пиццы"]
🤖 📝 Распознано: "Собери мне корзину для пиццы"
    [Находит ингредиенты для пиццы]
```

## 🔧 Конфигурация

### config.yaml

```yaml
telegram:
  bot_token: "..."              # Токен Telegram бота
  admin_ids: [123, 456]         # ID администраторов

llm:
  model: "..."                  # Модель LLM
  api_key: "..."                # API ключ
  api_base: "..."               # Базовый URL API

mcp:
  url: "..."                    # URL MCP сервера

bot:
  max_history_messages: 20      # Макс. сообщений в истории
  stream_update_interval: 1.0   # Интервал обновления стрима (сек)
  stream_min_chars: 50          # Мин. символов для обновления
```

### Промпты

Промпты хранятся в `prompts/`:

- **system_prompt.txt** — системный промпт для AI
- **user_initial_prompt.txt** — шаблон начального сообщения

Можно редактировать без изменения кода!

## 🎨 Особенности

### 📸 Vision & Multimodal

Бот поддерживает анализ изображений через OpenAI Responses API:

**Возможности:**
- 📷 Анализ фото холодильника и определение продуктов
- 🥗 Распознавание блюд на фотографиях
- 🛒 Автоматический подбор недостающих ингредиентов
- 💡 Предложения рецептов на основе имеющихся продуктов

**Формат сообщений:**
```python
{
    "role": "user",
    "content": [
        {"type": "input_text", "text": "Что приготовить?"},
        {"type": "input_image", "image_url": "data:image/jpeg;base64,..."}
    ]
}
```

**Особенности реализации:**
- Использует формат Responses API (`input_text`/`input_image`)
- Изображения кодируются в base64 и передаются через data URL
- Стриминг отключён для диалогов с фото (для стабильности)
- Контекст с изображением сохраняется в сессии до перезапуска

### Стриминг ответов

Бот поддерживает стриминг ответов в реальном времени:

1. Использует Bot API 9.3 `sendMessageDraft` (если доступно)
2. Fallback на `editMessageText` с защитой от flood control
3. Обновление каждые 50 символов или раз в секунду
4. **Автоматическое отключение** для диалогов с multimodal контентом

**Примечание:** Стриминг отключается при наличии изображений в истории из-за несовместимости форматов между Responses API и Chat Completions API.

### MCP Tools

Бот использует два инструмента через MCP:

1. **search_products(query)** — поиск товаров по названию
2. **create_cart(products_json)** — создание корзины и получение ссылки

### Контекстная память

- Хранит последние 20 сообщений (10 пар запрос-ответ)
- Отдельный контекст для каждого пользователя
- Поддержка топиков (отдельный контекст для каждой темы)

### Логирование и аналитика

**Структура логов** (`logs/`):
```
logs/
└── 2026-01-03/
    └── 123456789/
        ├── 2026-01-03_14-30-15.json
        └── 2026-01-03_14-32-45.json
```

Каждый лог содержит:
- `timestamp` — время взаимодействия
- `user_id`, `username` — данные пользователя
- `query` — запрос пользователя
- `response` — ответ бота
- `tools_used` — использованные инструменты (search_products, create_cart)
- `tokens` — статистика токенов (input, output, total, cache)
- `error` — текст ошибки (если была)

**База данных** (`db/`):
- `users.json` — все пользователи с метаданными
- `interactions.json` — статистика взаимодействий по датам

**API для работы с данными:**
```python
from bot.src.utils.logger import AgentLogger
from bot.src.utils.database import UserDatabase

# Логи
logger = AgentLogger()
logs = logger.get_user_logs(user_id=123456789, date="2026-01-03")

# База данных
db = UserDatabase()
stats = db.get_stats()  # {"total_users": 42, "active_today": 5, "total_interactions": 128}
user = db.get_user(123456789)
```

## 🔐 Безопасность

- Конфигурация в `.gitignore`
- Токены и API ключи не в коде
- SSL verification отключен только для MCP (внутренний сервис)
- Уведомления админов о всех запросах

## 📊 Мониторинг

### Логирование

```bash
# Docker
docker compose logs -f

# Локально
tail -f bot.log
```

### Метрики

Бот логирует:
- 👤 Запросы пользователей
- 🔍 Поиски товаров
- 🛒 Создание корзин
- 📊 Использование токенов
- ⚠️ Ошибки

## 🚢 Деплой

### Docker Compose

```yaml
services:
  vkusvill-bot:
    build: .
    container_name: vkusvill-bot
    restart: unless-stopped
    volumes:
      - ./app.py:/app/app.py
      - ./bot:/app/bot
      - ./config.yaml:/app/config.yaml
      - ./prompts:/app/prompts
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
```

### Управление

```bash
# Запуск
docker compose up -d

# Остановка
docker compose down

# Перезапуск
docker compose restart

# Логи
docker compose logs -f

# Обновление
git pull
docker compose up -d --build
```

## 🛠️ Разработка

### Добавление нового handler

1. Создать функцию в `bot/src/handlers/`
2. Добавить router
3. Зарегистрировать в `app.py`

### Добавление нового MCP tool

1. Добавить функцию в `bot/src/mcp/tools.py`
2. Декорировать `@function_tool`
3. Добавить в список tools

### Изменение промптов

Просто отредактируйте файлы в `prompts/`:
- `system_prompt.txt` — поведение AI
- `user_initial_prompt.txt` — начальное сообщение

Перезапустите бота для применения изменений.

## 🐛 Troubleshooting

### Бот не отвечает

1. Проверьте логи: `docker compose logs`
2. Проверьте токен бота в `config.yaml`
3. Убедитесь, что бот запущен: `docker compose ps`

### Ошибка "Flood control exceeded"

Это нормально при частых обновлениях. Бот автоматически снижает частоту.

### Топики не работают

1. Включите топики через @BotFather:
   - `/mybots` → выбрать бота
   - Bot Settings → Topics in Private Chats
2. Обновите Telegram до последней версии

### MCP ошибки

Проверьте доступность MCP сервера:
```bash
curl https://mcp001.vkusvill.ru/mcp
```

### Проблемы с изображениями

**Бот не видит фото:**
1. Убедитесь, что используется правильная версия OpenAI Agents SDK (>=0.6.4)
2. Проверьте логи на наличие ошибок `Unknown content`
3. Отправьте фото заново после перезапуска бота (сессии не сохраняются)

**Ошибки стриминга с multimodal:**
```
❌ Invalid user message at index X
```
Это нормально - стриминг автоматически отключается для диалогов с изображениями.

**Формат изображений:**
- Поддерживаются: JPEG, PNG, WebP
- Максимальный размер: определяется Telegram (обычно до 10MB)
- Передаются как base64 в data URL

### Whisper API ошибки

**Не распознаёт голосовые:**
1. Проверьте настройки `whisper` в `config.yaml`
2. Убедитесь, что Whisper API доступен
3. Проверьте размер и длительность голосового сообщения

**Ограничения:**
- Максимальный размер: 20 MB (настраивается)
- Максимальная длительность: 180 секунд (3 минуты)

## 📝 Лицензия

MIT

## 🤝 Контрибьюция

Pull requests приветствуются! Для крупных изменений создайте issue для обсуждения.

## 📧 Контакты

- GitHub: [maddness/vkusvill-mcp-bot](https://github.com/maddness/vkusvill-mcp-bot)
- Issues: [GitHub Issues](https://github.com/maddness/vkusvill-mcp-bot/issues)

## 🙏 Благодарности

- [OpenAI Agents SDK](https://github.com/openai/openai-agents-python)
- [LiteLLM](https://github.com/BerriAI/litellm)
- [aiogram](https://github.com/aiogram/aiogram)
- [ВкусВилл](https://vkusvill.ru/) за MCP API

---

**Версия:** 2.1.0  
**Дата обновления:** 03 января 2026  
**Bot API:** 9.3  
**Поддержка Vision:** ✅ (Responses API)

## 📋 История изменений

### v2.1.0 (03.01.2026)
- ✅ Добавлена поддержка Vision/Multimodal через Responses API
- ✅ Исправлен формат multimodal контента (`input_text`/`input_image`)
- ✅ Добавлена проверка multimodal в стриминге (автоотключение)
- ✅ Обновлён системный промпт с инструкциями про vision
- ✅ Исправлен монтаж `config.yaml` в Docker
- ✅ Добавлена поддержка Whisper API для голосовых сообщений

### v2.0.0 (31.12.2025)
- 🎉 Первая стабильная версия
- ⚡ Стриминг через Bot API 9.3
- 🛠️ MCP инструменты
- 📊 Логирование и аналитика