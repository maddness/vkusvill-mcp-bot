# VkusVill Telegram Bot

Telegram-бот для сбора продуктовых корзин ВкусВилл с использованием AI.

![Demo](assets/pic.jpg)

## Что делает бот

- Принимает запросы на сбор продуктов (например, "Собери корзину для салата оливье")
- Использует Claude AI для понимания рецептов и подбора ингредиентов
- Ищет товары через MCP (Model Context Protocol) ВкусВилл
- Формирует корзину и генерирует ссылку для оформления заказа

## Стек

- **OpenAI Agents SDK** — агентский фреймворк с поддержкой MCP
- **LiteLLM** — прокси для работы с разными LLM через OpenAI-совместимый интерфейс
- **aiogram** — асинхронный Telegram Bot API
- **Claude Haiku 4.5** — языковая модель
- **Docker** — контейнеризация

## Установка и запуск

### 1. Клонировать репозиторий

```bash
git clone https://github.com/maddness/vkusvill-mcp-bot.git
cd vkusvill-mcp-bot
```

### 2. Настроить конфигурацию

**Создать файл `config.yaml`:**

```bash
cp config.example.yaml config.yaml
nano config.yaml  # или любой другой редактор
```

**Заполнить токены и настройки:**

```yaml
telegram:
  bot_token: "YOUR_BOT_TOKEN"  # Токен от @BotFather
  admin_ids:
    - 123456789  # Ваш Telegram ID для уведомлений

llm:
  model: "litellm/openai/claude-haiku-4-5"
  api_key: "YOUR_API_KEY"  # API ключ для Claude
  api_base: "https://openai-hub.neuraldeep.tech/v1"

mcp:
  url: "https://mcp001.vkusvill.ru/mcp"

bot_settings:
  max_history_messages: 20
  stream_update_interval_seconds: 1.0
  stream_update_min_chars: 50
```

**Где взять токены:**
- `bot_token` — создайте бота через [@BotFather](https://t.me/BotFather) в Telegram
- `api_key` — получите API ключ для Claude или совместимого сервиса
- `admin_ids` — ваш Telegram ID (узнать можно через [@userinfobot](https://t.me/userinfobot))

### 3. Запуск через Docker (рекомендуется)

**Собрать и запустить контейнер:**

```bash
docker compose up -d --build
```

**Посмотреть логи:**

```bash
docker compose logs -f
```

**Остановить бота:**

```bash
docker compose down
```

**Перезапустить бота:**

```bash
docker compose restart
```

### 4. Или запуск локально

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python app.py
```

## Деплой на сервер

### Требования к сетевому доступу

Сервер должен иметь исходящий доступ к следующим ресурсам:

| Ресурс | Порт | Назначение |
|--------|------|------------|
| `github.com` | 443 | Клонирование репозитория |
| `pypi.org`, `files.pythonhosted.org` | 443 | Установка Python-пакетов |
| `api.telegram.org` | 443 | Telegram Bot API |
| `mcp001.vkusvill.ru` | 443 | MCP сервер ВкусВилл |
| `openai-hub.neuraldeep.tech` | 443 | API для Claude |

### 1. Подключиться к серверу
```bash
ssh user@your-server-ip
```

### 2. Установить Docker
```bash
sudo apt update
sudo apt install docker.io docker-compose-v2 -y
sudo usermod -aG docker $USER
# Перелогиниться для применения прав
```

### 3. Клонировать репозиторий
```bash
git clone https://github.com/maddness/vkusvill-mcp-bot.git
cd vkusvill-mcp-bot
```

### 4. Настроить конфигурацию
```bash
cp config.example.yaml config.yaml
nano config.yaml  # вставить токены (см. раздел "Настроить конфигурацию")
```

### 5. Запустить бота
```bash
docker compose up -d --build
```

### 6. Управление ботом

**Просмотр логов:**
```bash
# Последние 50 строк
docker compose logs --tail=50

# Следить за логами в реальном времени
docker compose logs -f
```

**Перезапуск бота:**
```bash
docker compose restart
```

**Остановка бота:**
```bash
docker compose down
```

**Обновление кода:**
```bash
git pull
docker compose up -d --build
```

**Просмотр логов взаимодействий:**
```bash
# Структура: logs/YYYY-MM-DD/user_id/timestamp.json
ls -la logs/
cat logs/2026-01-03/123456789/2026-01-03_14-30-15.json | jq .
```

**Просмотр базы данных пользователей:**
```bash
cat db/users.json | jq .
cat db/interactions.json | jq .
```

## Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Начать работу с ботом |
| `/new_chat` | Сбросить контекст и начать заново |

## Использование

1. Отправьте боту сообщение с описанием того, что хотите приготовить
2. Бот найдёт нужные продукты во ВкусВилл
3. Получите готовую корзину со ссылкой на оформление заказа
4. Нажмите кнопку "Собрать новую корзину" для нового запроса

## Структура проекта

```
vkusvill-mcp-bot/
├── app.py                      # Точка входа
├── config.yaml                 # Конфигурация (не в git)
├── docker-compose.yml          # Docker конфигурация
├── Dockerfile                  # Образ Docker
├── requirements.txt            # Python зависимости
├── prompts/                    # AI промпты (можно редактировать)
│   ├── system_prompt.txt       # Системный промпт
│   └── user_initial_prompt.txt # Начальный промпт
├── bot/src/
│   ├── handlers/               # Telegram обработчики
│   │   ├── commands.py         # Команды (/start, /new_chat)
│   │   └── messages.py         # Обработка сообщений
│   ├── agent/                  # AI Agent
│   │   └── runner.py           # Запуск агента
│   ├── mcp/                    # MCP клиент
│   │   ├── client.py           # HTTP клиент
│   │   └── tools.py            # Инструменты (search, create_cart)
│   └── utils/
│       ├── config.py           # Загрузчик конфигурации
│       ├── logger.py           # Логирование взаимодействий
│       └── database.py         # База данных пользователей
├── logs/                       # Логи взаимодействий (не в git)
│   └── YYYY-MM-DD/
│       └── user_id/
│           └── timestamp.json
├── db/                         # База данных (не в git)
│   ├── users.json
│   └── interactions.json
└── assets/                     # Ресурсы
```

## Возможности

- 🤖 **AI-ассистент** на базе Claude Haiku 4.5
- 🔍 **Автоматический поиск** товаров через MCP
- 🛒 **Генерация корзин** с готовой ссылкой
- ⚡ **Стриминг ответов** в реальном времени (Bot API 9.3)
- 📝 **Контекстная память** последних 20 сообщений
- 👥 **Уведомления админов** о всех запросах
- 📊 **Логирование** всех взаимодействий в JSON
- 💾 **База данных** пользователей и статистики

## Документация

📖 **[Полная документация в AGENT.md](AGENT.md)**

## Лицензия

MIT
